# [ProtoGuidance] E4-hardneg-fsc-nq600：最优配方 + num_queries 600
# 动机：MS 分解显示 145/172 漏检为"完全漏检"且集中在密集港口场景，
#       300 query 覆盖不足。600 query 需要 query 扩展加载支持
#       （weights.py _pad_query_rows：新增槽位随机初始化）。
# 对照：E4-hardneg-fsc（0.9380/0.1620，+阈值 0.9327/0.1304）。
# 用法: python src/scripts/train.py -c configs/experiments/proto_guidance_E4-hardneg-fsc-nq600.yaml
model:
  num_queries: 600   # 300→600：增加 decoder query 覆盖（MS 密集场景 145 完全漏检假设）
  num_classes: 25
  resolution: 1024
  gradient_checkpointing: true
  # 从当前最优模型（E4-hard-neg 配方 1024）续训
  pretrain_weights: output/0816-SHWX-ProtoGuidance-E4-hardneg-fsc/checkpoint_best_total.pth  # 当前最优（300 query，扩展加载）
  # --- 多模态原型引导：全开（位置 + 辅助损失 + 内容增强） ---
  proto_guidance_enabled: true
  proto_guidance_artifacts_path: data/proto_guidance_shwx.pt
  proto_guidance_fusion_mode: simple
  proto_guidance_num_slots: 10
  proto_guidance_position_enabled: true
  proto_guidance_content_enabled: true
  proto_guidance_aux_loss_enabled: true
  proto_guidance_aux_loss_weight: 1.0
  proto_guidance_warmup_epochs: 2.0
  proto_guidance_lambda_pos_init: 0.05
  proto_guidance_lambda_pos_max: 1.0
  proto_guidance_gamma_content_init: 0.05
  proto_guidance_gamma_content_max: 1.0
  proto_guidance_tau_p: 0.2

train:
  dataset_dir: /home/liu/wzt/datasets/SHWX-dataset-dict-redo
  dataset_file: yolo
  output_dir: output/0817-SHWX-ProtoGuidance-E4-hardneg-fsc-nq600
  epochs: 20
  batch_size: 16
  grad_accum_steps: 4
  num_workers: 6
  lr: 1e-5
  lr_encoder: 1e-5
  weight_decay: 1e-4
  clip_max_norm: 0.1
  lr_drop: 16
  warmup_epochs: 0.0
  tensorboard: true
  wandb: false
  device: cuda
  devices: 1
  num_nodes: 1
  resume: ""
  eval_interval: 1
  use_ema: true
  compute_val_loss: false
  aug_config: AERIAL
  mosaic_p: 0.0
  multi_scale: false
  class_balanced_sampling: true
  class_balanced_class_ids: [0, 1, 24]

  # --- FSC 正/负样本补丁粘贴（本轮新增） ---
  patch_paste_enabled: false
  patch_paste_dir: data/fsc_patch_pool   # 由 build_fsc_patch_pool.py 生成
  patch_paste_prob: 0.5
  patch_paste_max_patches: 2
  patch_paste_target_classes: [24]       # 宿主约束：只贴到含发射车的图
  patch_paste_neg_ratio: 0.5

  # --- SSCL 配置：沿用 E4 配方 ---
  sscl_enabled: true
  sscl_semantic_matrix_path: data/semantic_matrix_shwx.pt
  sscl_matrix_normalize: minmax
  sscl_lambda: 0.02
  sscl_tau: 0.1
  sscl_rho: 0.3
  sscl_omega_max: 2.0
  sscl_anchor_classes: [0, 1, 2, 3]
  sscl_confusing_classes: [0, 1, 2, 3]
  sscl_start_epoch: 0
  sscl_freeze_strategy: conservative
  sscl_unfreeze_decoder_layers: 2
  sscl_prototype_enabled: false

  # --- 难负样本抑制：noMS 目标集（FSC 不进 logit 抑制，交给补丁粘贴） ---
  sscl_hard_neg_enabled: true
  sscl_hard_neg_topk: 5
  sscl_hard_neg_score_thresh: -0.7
  sscl_hard_neg_log_interval: 100
  sscl_hard_neg_target_classes: [0, 1, 2, 24]   # FSC 加入（按类参数）
  sscl_hard_neg_iou_low: 0.0
  sscl_hard_neg_iou_high: 0.15
  sscl_hard_neg_loss_lambda: 0.3
  sscl_hard_neg_logit_margin: -1.5
  sscl_hard_neg_target_logit_margins: {24: -2.5}   # FSC 放宽
  sscl_hard_neg_target_loss_lambdas: {24: 0.15}  # FSC 减量
  sscl_hard_neg_logit_temperature: 1.0
  sscl_hard_neg_proto_lambda: 0.05
  sscl_hard_neg_proto_margin: 0.1
  sscl_hard_neg_proto_temperature: 0.1
