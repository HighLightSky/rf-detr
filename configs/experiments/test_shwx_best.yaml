# 当前最优提交配置：E4-hardneg-fsc（训练侧 FSC 按类抑制） + 逐类阈值（不含 FSC，训练已压）
# total 0.9327/0.1304（R-F 0.802），all FDR 0.0513；备选 0816+阈值 0.9375/0.1334

test:
  dataset: shwx
  dataset_dir: /home/liu/wzt/datasets/SHWX-dataset-dict-redo
  resolution: 1024
  checkpoint: output/0816-SHWX-ProtoGuidance-E4-hardneg-fsc/checkpoint_best_total.pth
  output_dir: /tmp/fsc_b_thresh_out
  conf_threshold: 0.25
  class_conf_thresholds: {0: 0.35, 2: 0.30, 3: 0.30, 7: 0.50, 10: 0.55, 16: 0.45, 17: 0.40, 18: 0.60, 22: 0.50}
  device: cuda:0
  batch_size: 32
  num_workers: 12
  prefetch_factor: 3
  gpu_util_sample_interval: 0.5
  use_fp16: false
  save_fp_fn: false
  save_yolo_preds: false
