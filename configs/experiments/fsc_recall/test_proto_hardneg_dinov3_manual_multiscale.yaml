# 人工 FSC 复核标注训练的 DINOv3 三尺度二阶段头完整测试配置。
# 正式测试仅用于一次独立评估，不使用其结果选取阈值或模型。
test:
  dataset: shwx
  dataset_dir: /home/liu/wzt/datasets/SHWX-FINAL-no-FSC-expand
  resolution: 1024
  checkpoint: output/0826_发射车细粒度检测/00_当前最佳方案/原型前向_难负样本抑制_DINOv3两级/checkpoint_best_total.pth
  output_dir: output/0826_发射车细粒度检测/00_当前最佳方案/原型前向_难负样本抑制_DINOv3两级/test_full_conf025_dinov3_manual_multiscale
  conf_threshold: 0.25
  ms_nms:
    enabled: false
  reason_plugin:
    enabled: false
  two_stage:
    enabled: true
    backend: dinov3
    checkpoint: output/0826_发射车细粒度检测/00_当前最佳方案/原型前向_难负样本抑制_DINOv3两级/人工标注二级头训练/fsc_dinov3_head_manual_multiscale_150_200_300.pth
    backbone_checkpoint: output/0826_发射车细粒度检测/00_当前最佳方案/原型前向_难负样本抑制_DINOv3两级/dinov3_vit_base_patch16_lvd1689m_backbone.pth
    class_ids: [24]
    candidate_floor: 0.05
    candidate_nms_iou: 0.5
    context_scale: 2.0
    image_size: 224
    batch_size: 63
    positive_threshold: 0.5
    detector_score_weight: 0.0
  device: cuda:0
  batch_size: 16
  num_workers: 12
  prefetch_factor: 3
  precision: auto
  compile_model: false
  copy_prefetch: true
  warmup_batches: 1
  progress_interval_s: 1.0
  gpu_monitor_enabled: false
  save_fp_fn: true
  save_yolo_preds: false
